pre-commit:
  parallel: true
  jobs:
    - name: "Lint staged files"
      run: npm run eslint {staged_files}
      glob: "*.{js,ts}"

    - name: "Run hello script"
      run: "npm run hello"
      # runner: node

    - name: "Run tests"
      run: npm test
